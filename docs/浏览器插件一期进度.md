# 浏览器插件一期进度

## 当前快照

- 状态：已收口（研发侧）
- 负责人：当前本地工作区
- 目标：浏览器沉浸式引导 MVP 已完成收口，并为未来 `Immersive-Input` 接入保留兼容空间

## 进度看板

### 1. 共享协议

- [x] 新增共享 Guide 协议
- [x] 从共享包导出 Guide 协议
- [x] 在扩展 `common` 层做二次导出
- 转入二期：Session 版本迁移策略

### 2. 浏览器运行时

- [x] 新增后台 Guide Runtime 监听器
- [x] 新增 Guide Session 持久化
- [x] 新增 `create / list / get / start / advance / cancel / attach-tab`
- [x] 新增 Session 变更事件广播
- 转入二期：更细粒度的错误分类

### 3. 浏览器 Overlay

- [x] 新增 Guide Overlay 内容脚本
- [x] 新增基于选择器的高亮渲染
- [x] 新增运行时面板渲染
- [x] 新增 `Next / Skip / End` 控制
- [x] 新增滚动与尺寸变化后的高亮跟随

### 4. 入口统一

- [x] 将内置聊天入口接入 Guide Runtime
- [x] 将外部 MCP 任务接入 Guide Runtime
- [x] 新增 popup 本地预览入口
- [x] 统一内外入口的任务载荷结构

### 5. Immersive-Input 预留

- [x] 在 Session 协议中保留集成提示字段
- [x] 定义浏览器到桌面端的共享桥接事件契约
- [x] 定义 handoff / resume 生命周期

## 一期收口结论

- Guide Runtime、Guide Session、Overlay MVP、popup 预览、内置聊天入口、外部 MCP 入口与桥接预留已全部落地。
- 浏览器插件主链路已收敛成可由 Guide Runtime 调度的能力池，前台入口完成收口，后台成熟能力保留可调度。
- `tests/record-replay-v3` 与 `tests/web-editor-v2` 的历史类型债已清理完成。
- `pnpm --filter chrome-mcp-server compile` 与 `pnpm --filter chrome-mcp-server build` 已通过。
- 一期不再继续扩展新功能，后续演进转入二期实施方案。

## 当前门槛状态

### 协议门槛

- [x] 共享协议已存在
- [x] 共享包导出已更新
- [x] 扩展公共层已二次导出

### 运行时门槛

- [x] 代码层面可创建 Session
- [x] Session 存储已存在
- [x] 运行时状态迁移处理器已存在
- [x] Overlay 派发链路已存在
- [x] Session 变更事件可对 UI 广播

### 页面门槛

- [x] Overlay 可展示
- [x] Overlay 可高亮目标元素
- [x] Overlay 可关闭
- [x] Overlay 支持步骤控制按钮

### 入口门槛

- [x] 外部 AI 入口已接 Runtime
- [x] 内置聊天入口已接 Runtime
- [x] popup 本地入口已能创建并启动 Guide Session
- [x] 共享任务适配层已完成

### 复用门槛

- [x] Session 协议已包含来源、目标、集成元数据
- [x] 浏览器到桌面端共享桥接契约已存在
- [x] `Immersive-Input` 桥接适配层已存在
- [x] Session 续跑 / 回放策略已定义

## 变更记录

### 初始落地

- 新增共享 Guide 协议
- 新增浏览器 Guide Runtime 存储与监听
- 新增页面 Overlay MVP
- 新增一期执行与进度文档

### 本地预览切片

- 新增 popup 本地引导预览入口
- 新增 Overlay 的 `Next / Skip / End`
- 新增运行时 Session 变更事件广播
- 新增浏览器到桌面端共享桥接类型

### 外部 MCP 切片

- 导出可复用的 Guide Runtime 直接操作函数
- 新增外部 MCP 的 immersive-guide 工具
- 将 MCP 触发的引导会话接入与 popup、内置聊天相同的 Runtime

### 桥接适配切片

- 新增持久化的浏览器到 `Immersive-Input` 桥接适配器与本地事件历史
- 新增 handoff / accept / resume 生命周期动作
- 新增 `bridge_state / bridge_events / request_handoff / accept_handoff / resume` MCP 动作

### 共享任务适配切片

- 新增统一的 Guide 任务载荷适配器，供 popup / chat 与 MCP 入口共用
- 去掉内外入口分别拼装 Session 创建 payload 的重复逻辑

## 收口验收结果

1. 扩展内部任一入口都已能无须手工拼消息创建 Guide Session。
2. Guide Session 已可在浏览器页面中完成可视化预览与高亮引导。
3. 点击 `Next / Skip / End` 后 Overlay 可正确更新。
4. Session 结构已兼容未来桌面端 handoff。
5. 一期代码库已满足编译通过与构建通过的交付门槛。

## 二期承接项

- Session 版本迁移策略
- 更细粒度的错误分类与错误恢复
- 云端连接模式与远端 MCP 调度
- 模板化、追溯与浏览器到桌面协同
